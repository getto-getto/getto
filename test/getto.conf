@import
    # required fields
    name      = qeremy/router
    link      = github.com
    # optional fields
    version   = master
    config    = composer.json
    ---
    name      = qeremy/couch
    link      = github.com
    version   = master
    config    = composer.json

